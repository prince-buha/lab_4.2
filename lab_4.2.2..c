#include <stdio.h>

int main() {
    float x, y, result;

    printf("Enter the value of x: ");
    scanf("%f", &x);
    
    printf("Enter the value of y: ");
    scanf("%f", &y);

    if (y == 0) {
        printf("Error: Division by zero is not allowed.\n");
    } else {
        result = (x / y) * 5;
        
		}
	}

